{
  "name": "smart-home-microservices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "start": "pnpm run -r start",
    "dev": "pnpm run -r dev"
  },
  "devDependencies": {
    "typescript": "^4.5.4"
  }
}
